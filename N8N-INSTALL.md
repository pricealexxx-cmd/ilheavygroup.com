# Установка n8n Self-Hosted

## 🚀 Быстрый запуск (рекомендуется)

Используйте готовый скрипт:

```bash
./start-n8n.sh
```

Или напрямую:

```bash
npx n8n
```

n8n будет доступен по адресу: **http://localhost:5678**

---

## 📦 Варианты установки

### Вариант 1: Запуск через npx (без установки)

```bash
npx n8n
```

**Плюсы:**
- Не требует прав администратора
- Всегда использует последнюю версию
- Не занимает место постоянно

**Минусы:**
- Скачивает при каждом запуске (можно кэшировать)

### Вариант 2: Глобальная установка через npm

```bash
sudo npm install -g n8n
n8n
```

**Плюсы:**
- Установка один раз
- Команда `n8n` доступна везде

**Минусы:**
- Требует sudo (права администратора)
- Занимает место на диске

### Вариант 3: Docker (если установлен)

```bash
docker run -it --rm \
  --name n8n \
  -p 5678:5678 \
  -v ~/.n8n:/home/node/.n8n \
  n8nio/n8n
```

**Плюсы:**
- Изоляция окружения
- Легко обновлять
- Не засоряет систему

**Минусы:**
- Требует установки Docker

---

## 🔧 Настройка

### Изменение порта

```bash
npx n8n --port 8080
```

### Сохранение данных

По умолчанию n8n сохраняет данные в `~/.n8n/`

Чтобы изменить путь:

```bash
export N8N_USER_FOLDER=/path/to/data
npx n8n
```

### Использование базы данных

Для продакшн рекомендуется PostgreSQL или MySQL.

```bash
export DB_TYPE=postgresdb
export DB_POSTGRESDB_HOST=localhost
export DB_POSTGRESDB_PORT=5432
export DB_POSTGRESDB_DATABASE=n8n
export DB_POSTGRESDB_USER=your_user
export DB_POSTGRESDB_PASSWORD=your_password

npx n8n
```

---

## 🔐 Безопасность

### Настройка базовой аутентификации

```bash
export N8N_BASIC_AUTH_ACTIVE=true
export N8N_BASIC_AUTH_USER=admin
export N8N_BASIC_AUTH_PASSWORD=your_secure_password

npx n8n
```

### Отключение Webhook UI (для безопасности)

```bash
export N8N_DISABLE_PRODUCTION_MAIN_PROCESS=true
npx n8n
```

---

## 📝 Автозапуск при загрузке системы (macOS)

Создайте LaunchAgent:

1. Создайте файл `~/Library/LaunchAgents/com.n8n.plist`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.n8n</string>
    <key>ProgramArguments</key>
    <array>
        <string>/usr/local/bin/npx</string>
        <string>n8n</string>
    </array>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
</dict>
</plist>
```

2. Загрузите агент:

```bash
launchctl load ~/Library/LaunchAgents/com.n8n.plist
```

---

## 🛑 Остановка n8n

Нажмите `Ctrl+C` в терминале, где запущен n8n.

---

## 🔍 Проверка статуса

Откройте в браузере: http://localhost:5678

Если видите интерфейс n8n - всё работает! ✅

---

## 📚 Дополнительная информация

- Официальная документация: https://docs.n8n.io/
- GitHub: https://github.com/n8n-io/n8n




